{
  "profiles": [
    {
      "id": "codex-safe-default",
      "agent_type": "codex",
      "label": "Codex Local CLI",
      "description": "Launches a supervised local Codex CLI runner. Prompts from the app execute through the installed codex command.",
      "command": [
        "{backend_python}",
        "-u",
        "{backend_root}/app/executors/agent_runtime.py",
        "--runner",
        "codex"
      ],
      "env": {},
      "workspace_required": true,
      "supports_initial_prompt": true
    },
    {
      "id": "gemini-safe-default",
      "agent_type": "gemini",
      "label": "Gemini Safe Default",
      "description": "Launches a safe local stub process that simulates a Gemini supervisor session.",
      "command": [
        "python",
        "-u",
        "-c",
        "import os,sys,time; print(f\"[{os.getenv('AGENT_PROFILE')}] workspace={os.getcwd()}\"); prompt=os.getenv('AGENT_INITIAL_PROMPT'); print(f\"initial_prompt={prompt}\" if prompt else 'initial_prompt=<none>'); sys.stdout.flush(); i=0\nwhile True:\n line=sys.stdin.readline()\n if not line:\n  time.sleep(0.2); i+=1\n  if i % 50 == 0: print('heartbeat:gemini'); sys.stdout.flush()\n  continue\n line=line.strip()\n if line.lower()=='exit': print('shutting down'); sys.stdout.flush(); break\n print(f'gemini received: {line}'); sys.stdout.flush()"
      ],
      "env": {},
      "workspace_required": true,
      "supports_initial_prompt": true
    }
  ]
}
