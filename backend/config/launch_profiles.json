{
  "profiles": [
    {
      "id": "gemini-safe-default",
      "agent_type": "gemini",
      "adapter_id": "gemini-cli",
      "label": "Gemini CLI (Default)",
      "description": "Launches a supervised Gemini CLI runtime through the vendor-neutral mobile control plane adapter layer.",
      "env": {},
      "workspace_required": true,
      "supports_initial_prompt": true,
      "metadata": {
        "default_model": "gemini-2.5-pro",
        "mcp_enabled": true,
        "quickstart": "Gemini-first workflow with slash commands and MCP-aware project tooling."
      }
    },
    {
      "id": "hermes-safe-default",
      "agent_type": "hermes",
      "adapter_id": "hermes-cli",
      "label": "Hermes Agent (WSL)",
      "description": "Launches Hermes Agent through WSL using the same supervised control-plane adapter flow.",
      "env": {},
      "workspace_required": true,
      "supports_initial_prompt": true,
      "metadata": {
        "quickstart": "Hermes Agent workflow through WSL on Windows. Run hermes setup inside WSL first."
      }
    },
    {
      "id": "codex-safe-default",
      "agent_type": "codex",
      "adapter_id": "codex-cli",
      "label": "Codex CLI",
      "description": "Launches a supervised Codex CLI runtime through the same adapter interface used by Gemini CLI.",
      "env": {},
      "workspace_required": true,
      "supports_initial_prompt": true,
      "metadata": {
        "quickstart": "General-purpose Codex CLI workflow."
      }
    }
  ]
}
